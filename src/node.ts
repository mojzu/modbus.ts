/// <reference types="node" />
// Node and other dependencies.
import * as assert from "assert";
export { assert };
import { Buffer } from "buffer";
export { Buffer };
import { Server, createServer, Socket, createConnection } from "net";
export { Server, createServer, Socket, createConnection };
const debug = require("debug");
export { debug };
